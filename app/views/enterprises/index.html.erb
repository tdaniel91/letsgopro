<h1>Lista de empresas</h1>

<p>Tem um pequeno negocio e gostava de aumentar a sua visibilidade? <%= link_to "Crie já uma página", new_enterprise_path %> personalizavel para a sua empresa!</p>
<%= link_to 'New Enterprise', new_enterprise_path %>

<table>
  <thead>
  <tr>
    <th>Avatar</th>
    <th>Detalhes</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>
  </thead>

  <tbody>
  <% @enterprises.each do |enterprise| %>
      <tr>
        <td>
          <%= link_to (image_tag enterprise.attachment.url,  :class =>"imgUser", :alt => "enterprise img"), enterprise%>
        </td>
        <td>
          <%= link_to enterprise.name, enterprise %><br>
          <%= enterprise.slogan %>
          <%= enterprise.foundation.strftime("%d/%m/%Y") %>  </td>
		 <% if (Person.find(current_user.id).id == enterprise.admin_id) %>
        <td><%= link_to 'Ver', enterprise %></td>
        <td><%= link_to 'Editar', edit_enterprise_path(enterprise) %></td>
        <td><%= link_to 'Apagar', enterprise, method: :delete, data: { confirm: 'Tem a certeza?' } %></td>
		<% end %>
      </tr>
  <% end %>
  </tbody>
</table>
<br>


