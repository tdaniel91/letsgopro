<%= form_for(@contact) do |f| %>
  <% if @contact.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@contact.errors.count, "error") %> prohibited this contact from being saved:</h2>

      <ul>
      <% @contact.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


    <%#  <p> Deseja adicionar <b><%= Person.find(params[:user2_id]).name % ></b> à sua lista de contactos?</p> %>

    <br/><%= link_to 'Add as Friend' , new_contact_path(:user2_id => params[:user2_id]), :method => :post %>

    <% @u2 = params[:user2_id] %>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :user2_id %><br>
    <%= f.number_field :user2_id %>
  </div>
  <div class="field">
    <%= f.label :state %><br>
    <%= f.text_field :state %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
